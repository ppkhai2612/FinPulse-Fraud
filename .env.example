# Copy to .env then `make up`.
#
# Extra pip packages installed into the Airflow containers at startup.
# Leave empty unless you need pyspark, kafka-python, etc. inside DAGs.
_PIP_ADDITIONAL_REQUIREMENTS=